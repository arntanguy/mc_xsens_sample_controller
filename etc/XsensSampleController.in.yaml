---
# If true, the FSM transitions are managed by an external tool
Managed: false
# If true and the FSM is self-managed, transitions should be triggered
StepByStep: true
# Change idle behaviour, if true the state is kept until transition,
# otherwise the FSM holds the last state until transition
IdleKeepState: true
# Where to look for state libraries
StatesLibraries:
- "@MC_STATES_DEFAULT_RUNTIME_INSTALL_PREFIX@"
- "@MC_STATES_RUNTIME_INSTALL_PREFIX@"
# Where to look for state files
StatesFiles:
- "@MC_STATES_DEFAULT_RUNTIME_INSTALL_PREFIX@/data"
- "@MC_STATES_RUNTIME_INSTALL_PREFIX@/data"
# If true, state factory will be more verbose
VerboseStateFactory: false
# Additional robots to load
robots:
  ground:
    module: env/ground
# General constraints, always on
constraints:
- type: contact
#- type: dynamics
#  damper: [0.1, 0.01, 0.5]
- type: compoundJoint
# Collision constraint
collisions:
- type: collision
  useMinimal: true
# Initial set of contacts
contacts: []
# - r1: jvrc1
#   r2: ground
#   r1Surface: LeftFoot
#   r2Surface: AllGround
# - r1: jvrc1
#   r2: ground
#   r1Surface: RightFoot
#   r2Surface: AllGround

# Some options for a specific robot
jvrc1:
  posture:
    stiffness: 1
    weight: 10
  ff:
    stiffness: 2
    weight: 100
# Implement some additional text states
states: {}
# Transitions map
transitions:
- [XsensSampleController_Initial, OK, XsensRetargetting, Strict]
# Initial state
init: XsensSampleController_Initial

Plugins: [XsensPlugin]
Xsens:
  jvrc1:
    "PELVIS_S": "Pelvis"
    "NECK_Y_S": "Neck"
    "NECK_P_S": "Head"
    "R_SHOULDER_P_S": "Right Shoulder"
    "R_SHOULDER_Y_S": "Right Upper Arm"
    "R_ELBOW_P_S": "Right Forearm"
    "R_WRIST_Y_S": "Right Hand"
    "L_SHOULDER_P_S": "Left Shoulder"
    "L_SHOULDER_Y_S": "Left Upper Arm"
    "L_ELBOW_P_S": "Left Forearm"
    "L_WRIST_Y_S": "Left Hand"
    # "L5": 1
    # "L3": 2
    # "T12": 3
    # "T8": 4
    # "Right Upper Leg": 15
    # "Right Lower Leg": 16
    # "Right Foot": 17
    # "Right Toe": 18
    # "Left Upper Leg": 19
    # "Left Lower Leg": 20
    # "Left Foot": 21
    # "Left Toe": 22
    # "23": 23
    # "Prop1": 24
    # "Prop2": 25
    # "Prop3": 26
    # "Prop4": 27